# NSW PSN External API Proxy - Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# DATABASE (PostgreSQL)
# =============================================================================

# PostgreSQL connection URL for cache, stats, config, and history data
# Format: postgresql://user:password@host:port/database
# DATABASE_URL=postgresql://nswpsn:yourpassword@localhost:5432/nswpsn

# =============================================================================
# API KEYS
# =============================================================================

# NSWPSN API key for /api endpoints (used by clients to authenticate)
NSWPSN_API_KEY=your_api_key_here

# Pagermon API key (optional - only if your Pagermon requires auth)
# PAGERMON_API_KEY=

# =============================================================================
# SUPABASE (required for user auth and incidents)
# =============================================================================

# Supabase project URL
SUPABASE_URL=https://your-project.supabase.co

# Supabase anon key (for frontend)
SUPABASE_KEY=your_supabase_anon_key_here

# Supabase service role key (for backend admin operations like creating users)
# Find this in: Supabase Dashboard > Settings > API > service_role key
# WARNING: Keep this secret! Never expose in frontend code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# =============================================================================
# ENDEAVOUR ENERGY (Supabase-based API)
# =============================================================================

# Endeavour Energy's public Supabase REST API
ENDEAVOUR_SUPABASE_URL=https://your-endeavour-project.supabase.co/rest/v1
ENDEAVOUR_SUPABASE_KEY=your_endeavour_anon_key_here

# =============================================================================
# EXTERNAL API URLS (optional - defaults shown)
# =============================================================================

# Pagermon API URL
# PAGERMON_URL=https://pager.forcequit.xyz/api/messages

# Proxy for Waze API requests (Waze blocks datacenter IPs)
# Single proxy:
# WAZE_PROXY_URL=http://host:port
# Rotating proxy pool (comma-separated, one proxy per region request):
# WAZE_PROXY_LIST=host1:port,host2:port,host3:port
# WAZE_PROXY_PROTO=http          # Protocol for WAZE_PROXY_LIST entries (default: http)

# =============================================================================
# CACHE TTL SETTINGS (optional - in seconds)
# =============================================================================

# CACHE_TTL_PAGER=120       # Default: 2 min
# CACHE_TTL_WEATHER=300     # Default: 5 min
# CACHE_TTL_STATS=60        # Default: 1 min
# CACHE_TTL_RSS=300         # Default: 5 min

# =============================================================================
# DATA RETENTION (optional)
# =============================================================================

# DATA_RETENTION_DAYS=7     # How long to keep historical data
# DATA_CLEANUP_INTERVAL=3600  # Cleanup check interval (seconds)

# =============================================================================
# DISCORD NOTIFICATIONS
# =============================================================================

# Discord webhook for editor request notifications
# Create a webhook in your Discord server and paste the URL here
# EDITOR_REQUEST_WEBHOOK=https://discord.com/api/webhooks/...

# Discord user ID to ping when new editor requests come in (optional)
# Right-click your profile in Discord with Developer Mode enabled to copy your ID
# EDITOR_REQUEST_PING_ID=123456789012345678

# =============================================================================
# SERVER SETTINGS (optional)
# =============================================================================

# DEV_MODE=false            # Set to true for verbose logging
